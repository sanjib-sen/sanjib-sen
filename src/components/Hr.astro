---
export interface Props {
  noPadding?: boolean;
  ariaHidden?: boolean;
}

const { noPadding = false, ariaHidden = true } = Astro.props;
---

<div
  class:list={[`max-w-3xl mx-auto`, { "px-0": noPadding, "px-4": !noPadding }]}
>
  <hr class="border-skin-line" aria-hidden={ariaHidden} />
</div>
